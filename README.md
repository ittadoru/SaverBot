
# SAVEBOT

**SAVEBOT** — это Telegram-бот для скачивания видео из популярных соцсетей и работы с подписками.  
Бот написан на **Python (aiogram 3 + FastAPI)** и использует **PostgreSQL** для хранения данных.  

Весь проект упакован в Docker-контейнеры для удобного запуска и развёртывания.

---

## Быстрый поиск

- [🚀 Запуск проекта](#-запуск-проекта)
- [💎 Особенности интерфейса](#-особенности-интерфейса)
- [🔒 Безопасность и приватность](#-безопасность-и-приватность)
- [🗂️ Структура проекта](#️-структура-проекта)
- [📥 Скачивание видео](#-скачивание-видео)
- [⬆️ Обновление yt-dlp и pytubefix](#️-обновление-yt-dlp-и-pytubefix)
- [🧩 Расширяемость](#-расширяемость)
- [⛔️ Возможные проблемы](#⛔️-возможные-проблемы)
- [🛑 Ограничения и лимиты](#-ограничения-и-лимиты)
- [⚠️ Важно: настройка username бота в шаблонах](#️-важно-настройка-username-бота-в-шаблонах)
- [ℹ️ Дополнительно](#️-дополнительно)
- [Связь с разработчиком](#связь-с-разработчиком)

---

## 🚀 Запуск проекта
В репозитории намеренно НЕТ файлов миграций в `alembic/versions/` (директория пуста), чтобы не тащить историю изменений. Схему можно создать автоматически.

```bash
git clone https://github.com/ittadoru/BotTemplate.git
cd BotTemplate
cp .env.example .env        # заполнить значения
docker compose up -d --build
docker compose logs -f app  # смотреть логи
```

После запуска:
- Бот становится доступен в Telegram (по токену из `.env`).  
- Webhook для оплаты через YooKassa: `https://DOMAIN/yookassa`

---

## ⛔️ Возможные проблемы
Если выходит ошибка, связанная с миграциями и sqlalchemy, то следует остановить контейнер, удалить базу данных в volume, удалить все миграции в папке alembic/versions (если есть), и в контейнере posgresql в ручную создать миграцию с нуля:
```bash
docker compose down
docker volume ls 
# допустим volume называется pgdata
docker volume rm pgdata
docker compose run --rm app bash
alembic revision --autogenerate -m "initial"
exit
docker compose up --build
```
А если нужно проверить данные в базе, то нужно дойдя до `docker compose run --rm app bash` далее ввести `psql -h postgres -U <ваш_пользователь> -d <ваша_бд>` и введя пароль делать запросы к бд

---

## 💎 Особенности интерфейса
- Кастомные премиальные страницы ошибок (404 для видео и общих страниц) — даже при ошибках пользователь видит стильный элитный дизайн.
- Все шаблоны адаптированы под мобильные устройства.

---

## 🔒 Безопасность и приватность
- Защита от повторных платежей (idempotency) реализована на уровне БД и webhook.
- Все временные ссылки приватны и имеют ограниченный срок жизни.
- Ошибки обрабатываются аккуратно, без утечек чувствительной информации.

---

## 🗂️ Структура проекта
- `handlers/` — обработчики команд и логики Telegram-бота (user, admin, support)
- `db/` — работа с базой данных (модели, запросы, миграции)
- `services/` — интеграции с платформами (YouTube, TikTok, Instagram и др.)
- `templates/` — HTML-шаблоны для страниц и ошибок
- `utils/` — вспомогательные функции (логгер, оплата, отправка файлов)
- `server.py` — FastAPI сервер, webhook, обработка платежей

---

## ⬆️ Обновление yt-dlp и pytubefix
Для корректной работы скачивания рекомендуется периодически обновлять yt-dlp и pytubefix внутри контейнера:

```bash
docker compose exec app pip install -U yt-dlp pytubefix
```

---

## 🧩 Расширяемость
- Для добавления новых платформ создайте новый модуль в `services/` и реализуйте нужный класс/метод.
- Проект легко масштабируется под новые источники медиа.

---

## ⚠️ Важно: настройка username бота в шаблонах
В шаблонах (`templates/`) ссылки на бота содержат плейсхолдер `YOUR_BOT_USERNAME`. После запуска обязательно замените его на актуальный username вашего бота, чтобы кнопки работали корректно.

---

## 📥 Скачивание видео
SAVEBOT поддерживает загрузку медиа из популярных сервисов:  
- **YouTube** — через `yt-dlp`.  
- **TikTok** — через `yt-dlp`.  
- **Instagram** — через `yt-dlp`.  

Видео сохраняется во временную директорию и отправляется пользователю прямо в чат.  

---

## ⚠️ Проблемы и решения
| Проблема                     | Возможная причина                  | Решение                                     |
|------------------------------|------------------------------------|---------------------------------------------|
| Видео не скачивается         | Устаревшая версия `yt-dlp`         | Обновите библиотеку                         |
| 400 ошибка `/yookassa`       | Неверный webhook URL               | Проверьте DOMAIN и настройки YooKassa       |
| Подписка не продлевается     | Тариф не найден в базе             | Проверьте миграции и таблицу тарифов        |
| Дублирование продлений       | Повторный webhook                  | Используйте idempotency key                 |
| Нет подключения к БД         | Postgres не запущен / неверный URL | Проверьте контейнер `postgres`              |
| Бот не отвечает              | Неверный BOT_TOKEN                 | Пересоздайте токен у BotFather              |

---

## 🛑 Ограничения и лимиты

| Категория         | Лимит скачиваний/сутки | Макс. размер файла | Время жизни ссылки | Реклама | Подписка на каналы | Качество YouTube | Прочее |
|-------------------|------------------------|--------------------|--------------------|---------|--------------------|------------------|--------|
| Обычный           | 10                     | 150 МБ (видео до 50 МБ — файл, больше — ссылка) | 5 минут           | Может быть        | Обязательна (если включена) | Только 480p/360p, аудио | Одновременное скачивание 1 файла |
| Подписчик         | 50                     | 1050 МБ            | 15 минут           | Нет     | Не требуется       | Любое, аудио     | Максимальные лимиты |
| Реферал 2 ур.     | 15                     | 150 МБ             | 5 минут            | Может быть  | Обязательна (если включена) | Только 480p/360p, аудио | |
| Реферал 3 ур.     | 20                     | 450 МБ              | 5 минут           | Может быть     | Обязательна (если включена)       | Только 480p/360p, аудио | |
| VIP (4, 5 - уже подписчик) | 50                | 1050 МБ            | 15 минут           | Нет     | Не требуется       | Только 480p/360p, аудио | Нет выбора качества, все остальные бонусы подписчика |

- Для всех платформ (YouTube, TikTok, Instagram и др.) действуют аналогичные лимиты по размеру и количеству скачиваний.
- Если размер файла превышает лимит — скачивание не происходит, выводится предупреждение.
- Одновременно можно скачивать только один файл.

---

## ℹ️ Дополнительно
Структура обработчиков (`handlers`) и пример админ-панели описаны в отдельном репозитории-шаблоне:  
👉 [BotTemplae](https://github.com/ittadoru/BotTemplae)  

---

## Связь с разработчиком
Если нужны доработки / вопросы / баги:
* Telegram: @ittadoru